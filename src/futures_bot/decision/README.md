# Decision Boundary

The decision layer creates `DecisionIntent` and `NoTradeDecision`.

It does not create orders directly. It does not mutate Ledger. It does not bypass RiskGate.

This module is a placeholder for future decision runtime.
